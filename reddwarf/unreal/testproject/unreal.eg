
include( <iostream> );

dim int m_unrealInt;

action UnrealTest[ 4 ]
{
    for( int i = 0; i < 100; ++i )
    {
        std::cout << "UnrealTest: " << i << std::endl;
        co_yield eg::sleep();
    }
}

function testUnreal : const char*
{
    return "Hello from Unreal";
}

LOG( "Unreal root created" );
